import { StyleSheet } from 'react-native';

export default StyleSheet.create({
	wrapper: { width: '100%', padding: 30 },
	panel: { position: 'absolute', left: '50%', top: -50, transform: 'translateX(-15px)' }
});
